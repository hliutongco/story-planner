import React from 'react';
import {connect} from 'react-redux'

const CharacterList = (props) => {

  return (
    <div className="character-list">
      this is the character list
    </div>
  );
}


export default connect(null)(CharacterList);

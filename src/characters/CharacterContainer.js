import React from 'react';
import {connect} from 'react-redux'

const CharacterContainer = (props) => {

  return (
    <div className="character-container">
      this is the character container
    </div>
  );
}


export default connect(null)(CharacterContainer);
